# Agent Memory

Running notes the agent platform has learned. This file is human-readable and
append-friendly; structured facts live in `state.db`.

## Seed facts
- The eval gate is Ninja Harness; runs below the profile threshold are flagged WARN.
- Browser answers must be concise — raw logs/headlines go in artifacts, not the answer.
- Never include secrets/tokens in any output or message.

<!-- The runner appends timestamped notes below. -->
